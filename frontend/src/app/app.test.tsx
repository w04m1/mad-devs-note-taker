import { QueryClient,QueryClientProvider } from "@tanstack/react-query"; import { render,screen } from "@testing-library/react"; import { MemoryRouter } from "react-router-dom"; import { describe,expect,it,vi } from "vitest"; import { ToastProvider } from "../components/toast"; import { App } from "./app";
vi.mock("../realtime/use-realtime",()=>({useRealtime:vi.fn()}));
describe("application routes",()=>{it.each([["/calendar","Calendar"],["/upcoming","Upcoming"],["/trash","Trash"],["/notifications","Notifications"],["/missing","Page not found"]])("renders %s",(path,title)=>{const client=new QueryClient({defaultOptions:{queries:{retry:false}}});render(<QueryClientProvider client={client}><MemoryRouter initialEntries={[path]}><ToastProvider><App/></ToastProvider></MemoryRouter></QueryClientProvider>);expect(screen.getByRole("heading",{name:title})).toBeInTheDocument()})});
