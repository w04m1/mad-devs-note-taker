import {afterEach,describe,expect,it,vi} from "vitest";import {act,render} from "@testing-library/react";import {QueryClient,QueryClientProvider} from "@tanstack/react-query";import {useUpcomingRefresh} from "./upcoming-view";
function Harness(){useUpcomingRefresh({today:[],this_week:[],past:[],server_now:"2026-01-01T10:00:00Z",next_transition_at:"2026-01-01T10:00:30Z"},"UTC");return null}
afterEach(()=>vi.useRealTimers());describe("upcoming refresh scheduler",()=>{it("refreshes at the transition, on focus, and with the minute fallback",()=>{vi.useFakeTimers();const client=new QueryClient();const invalidate=vi.spyOn(client,"invalidateQueries").mockResolvedValue(undefined);const view=render(<QueryClientProvider client={client}><Harness/></QueryClientProvider>);act(()=>vi.advanceTimersByTime(30000));expect(invalidate).toHaveBeenCalledTimes(1);act(()=>window.dispatchEvent(new Event("focus")));expect(invalidate).toHaveBeenCalledTimes(2);act(()=>vi.advanceTimersByTime(30000));expect(invalidate).toHaveBeenCalledTimes(3);view.unmount();act(()=>vi.advanceTimersByTime(60000));expect(invalidate).toHaveBeenCalledTimes(3)})});
