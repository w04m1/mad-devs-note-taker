import { QueryClient } from "@tanstack/react-query"; import { describe,expect,it,vi } from "vitest"; import { applyEvent,invalidationKeys,parseRealtimeEvent } from "./events";
const event={event_id:"e",type:"note.updated" as const,occurred_at:"2026-01-01T00:00:00Z",entity_id:"n",version:2,series_id:null};describe("realtime events",()=>{it("rejects malformed and unknown messages",()=>{expect(parseRealtimeEvent("nope")).toBeNull();expect(parseRealtimeEvent(JSON.stringify({...event,type:"unknown"}))).toBeNull()});it("maps note changes to dependent views",()=>{expect(invalidationKeys(event.type)).toEqual(expect.arrayContaining([["notes"],["calendar"],["upcoming"],["trash"]]))});it("requests a full invalidation for resync",()=>{const client=new QueryClient();const invalidate=vi.spyOn(client,"invalidateQueries");applyEvent(client,{...event,type:"resync_required"});expect(invalidate).toHaveBeenCalledWith()})});
