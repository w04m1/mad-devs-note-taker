# Notetaker frontend

Prerequisites: Node.js 22 and pnpm 11.

```sh
pnpm install --frozen-lockfile
pnpm dev
```

Vite proxies `/api` and `/ws` to the backend service. Use `pnpm typecheck`, `pnpm test`, and `pnpm build` before committing.
